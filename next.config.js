/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
        outputStandalone: true,
    },
    // presets: ["next/babel"],
};

module.exports = nextConfig;
